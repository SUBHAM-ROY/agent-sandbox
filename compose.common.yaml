services:
  base:
    user: "1000:1000"
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    read_only: true
    init: true
    mem_limit: 4g
    cpus: 2
    pids_limit: 2048
    stdin_open: true
    tty: true
    working_dir: ${PWD}
    volumes:
      - ${PWD}:${PWD}:rw
      - ${GIT_COMMON_DIR}:${GIT_COMMON_DIR}:rw
      # nix cache/state on disk, not tmpfs
      - /home/sandbox/.cache
      - /home/sandbox/.local/state
      - /nix
    tmpfs:
      - /tmp:mode=1777,size=512m
